CREATE TABLE users (
   row INT NOT NULL AUTO_INCREMENT,
  id VARCHAR(36) PRIMARY KEY,
  username VARCHAR(100) UNIQUE NOT NULL,
  email VARCHAR(50) UNIQUE NOT NULL,
  password VARCHAR(100) NOT NULL,
  UNIQUE KEY (row) -- required for AUTO_INCREMENT
);
